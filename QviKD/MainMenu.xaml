<UserControl x:Class="QviKD.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QviKD"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="MainMenu_Loaded">

    <UserControl.Resources>
        <ControlTemplate x:Key="rcMainMenuScroll" TargetType="ScrollBar" />
    </UserControl.Resources>

    <ScrollViewer Name="MainMenuScroll" ScrollViewer.VerticalScrollBarVisibility="Auto">
        <Grid Name="MainMenuContent" MaxWidth="900" Margin="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
        </Grid>
    </ScrollViewer>

</UserControl>
