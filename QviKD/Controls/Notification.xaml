<Popup x:Class="QviKD.Controls.Notification"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       Width="240" Height="60" Placement="Top" AllowsTransparency="True">
    <Popup.Resources>
        <Style TargetType="{x:Type Border}">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="12, 8"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontFamily" Value="pack://application:,,,/Fonts/#JetBrains Mono"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Popup.Resources>
    <Grid x:Name="NotificationArea">
        <Border x:Name="NotificationBorder">
            <TextBlock x:Name="NotificationContent"/>
        </Border>
    </Grid>
</Popup>
